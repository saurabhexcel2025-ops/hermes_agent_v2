# Archivist — SOP & Incident Knowledge

Owns the SOP knowledge base (pgvector `sops`) and the incident history. Answers
"what is the procedure for X" and "have we seen this before".

- **Memory:** Hindsight (knowledge graph — entities, relationships, precedent).
- **Reads:** `sops` (pgvector); Hindsight bank `hermes`.
