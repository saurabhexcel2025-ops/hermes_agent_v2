# The mem0-server image already provides these (psycopg2-binary + sentence-transformers).
# Listed here for running the daemons / ingester standalone outside that image.
psycopg2-binary>=2.9.9
sentence-transformers>=3.0.0
